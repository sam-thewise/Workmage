# API base URL (no /api/v1 suffix — it is appended)
# VITE_API_URL=http://localhost:8000

# Founders view only: when set to true/1, nav hides "My AI Roles" and "Admin".
# Use for deployments where only founder-facing menu options should appear.
# VITE_FOUNDERS_VIEW_ONLY=false
