apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: agent-foundry
  namespace: agent-foundry
  annotations:
    # For Azure: use application-gateway-ingress or nginx
    # kubernetes.io/ingress.class: nginx
spec:
  rules:
    - host: api-demo.example.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: api
                port:
                  number: 8000
    - host: demo.example.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: frontend
                port:
                  number: 5173
  # tls:
  #   - hosts: [api-demo.example.com, demo.example.com]
  #     secretName: agent-foundry-tls
